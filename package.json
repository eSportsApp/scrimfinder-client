{
  "name": "@esportsapp/client",
  "version": "1.0.2",
  "main": "index.js",
  "files": [
    "dist"
  ],
  "module": "src/client.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun run src/client.ts",
    "build": "bun build src/client.ts --target node",
    "dev": "bun run --watch tests/test.ts",
    "test": "bun run --watch client.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}